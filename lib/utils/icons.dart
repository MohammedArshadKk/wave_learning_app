import 'package:flutter/material.dart';

class AppIcons {
  static Icon menuIcon = const Icon(
    Icons.menu,
    color: Colors.white,
  );
  static Icon searchIcon = const Icon(
    Icons.search,
    color: Colors.white,
  );
  static Icon uploadIcon = const Icon(
    Icons.upload_rounded,
    color: Colors.white,
  );
}
